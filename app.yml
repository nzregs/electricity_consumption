install:
  commands:
    - ["npm", "ci", "--include=dev"]

run:
  command: ["node", ".next/standalone/server.js"]

profile:
  label: "Home Energy Dashboard"
  description: "Electricity consumption and export visualisation with tariff breakdown"
  icon: icon.svg
